import { AdminPanel } from '@/components/admin-panel'

export const metadata = {
  title: 'Admin Panel | SR Gateway',
  description: 'SR Gateway wallet administration panel.',
}

export default function AdminPage() {
  return <AdminPanel />
}
